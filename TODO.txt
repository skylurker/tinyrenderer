–асписать дл€ себ€ (может, просто протрассировать):

 1) ѕеременна€ error даЄт нам дистанцию до идеальной пр€мой от нашего текущего пиксел€ (x, y).  аждый раз, как error превышает один пиксель, мы увеличиваем (уменьшаем) y на единицу, и на единицу же уменьшаем ошибку.
int dx = x1-x0;
    int dy = y1-y0;
    float derror = std::abs(dy/float(dx));
    float error = 0;
    int y = y0;
    for (int x=x0; x<=x1; x++) {
        if (steep) {
            image.set(y, x, color);
        } else {
            image.set(x, y, color);
        }
        error += derror;

        if (error>.5) {
            y += (y1>y0?1:-1);
            error -= 1.;
        }
    }



2) –азобрать парсер model.cpp и прописать подробные комменты